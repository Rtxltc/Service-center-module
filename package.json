{
  "name": "motorola-laptop-service-center",
  "version": "1.0.0",
  "description": "Motorola and Laptop Service Center Website",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  }
}
